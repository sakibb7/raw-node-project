{
  "scripts": {
    "start": "SET \"NODE_ENV=staging\" && node index",
    "production": "SET \"NODE_ENV=production\" && node index"
  },
  "dependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.1.0",
    "prettier": "^3.2.5"
  }
}
